# 🔄 Міграція проекту з Bolt.new на власний Supabase

## 🎯 Проблема

Ви створили проект на **Bolt.new**, який використовує Supabase проект на їхньому акаунті:
```
URL: https://biwyivgaojbbnxdaxkwa.supabase.co
```

**Це НЕ ваш проект!** Ви не маєте до нього доступу і не можете:
- ❌ Деплоїти Edge Functions
- ❌ Керувати базою даних
- ❌ Бачити логи і статистику
- ❌ Bolt може видалити проект будь-коли

## ✅ Рішення

Створити **власний** Supabase проект і перенести туди структуру бази даних.

---

## 📋 Покрокова інструкція

### Крок 1: Створити власний проект на Supabase

1. **Перейдіть на** [supabase.com](https://supabase.com)

2. **Зареєструйтесь / Увійдіть**
   - Можна через GitHub (найшвидше)
   - Або через email

3. **Створіть новий проект:**
   - Натисніть **"New project"**
   - Заповніть форму:
     ```
     Organization: Ваша організація (створить автоматично якщо перший раз)
     Project Name: building-materials-portal (або будь-яка назва)
     Database Password: *** (придумайте надійний пароль і ЗБЕРЕЖІТЬ його!)
     Region: Europe Central (eu-central-1) - найближче до України
     Pricing Plan: Free
     ```
   - Натисніть **"Create new project"**

4. **Зачекайте 2-3 хвилини** поки проект створюється

---

### Крок 2: Отримати credentials нового проекту

Після створення проекту:

1. **Перейдіть в** Settings → API (ліва панель)

2. **Скопіюйте:**
   ```
   Project URL: https://xxxxx.supabase.co
   anon public key: eyJhbGc...
   Project Ref ID: xxxxx (це частина URL)
   ```

3. **Збережіть ці дані** - вони знадобляться далі!

---

### Крок 3: Застосувати SQL міграцію

У вас вже є готова SQL міграція в файлі:
```
supabase/migrations/20251204181532_create_building_materials_portal_schema.sql
```

**Спосіб 1: Через Supabase Dashboard (найпростіше)**

1. Перейдіть в **SQL Editor** (ліва панель)

2. Натисніть **"+ New query"**

3. **Скопіюйте весь вміст** файла `supabase/migrations/20251204181532_create_building_materials_portal_schema.sql`

4. **Вставте** в SQL Editor

5. Натисніть **"Run"** (або Ctrl+Enter)

6. Побачите повідомлення: **"Success. No rows returned"**

7. **Перевірте:** Table Editor → побачите всі таблиці:
   - profiles
   - categories
   - products
   - equipment
   - orders
   - order_items
   - rentals
   - cart_items

**Спосіб 2: Через Supabase CLI (для досвідчених)**

```bash
# Встановити Supabase CLI (якщо ще не встановлений)
# Windows (через scoop):
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase

# Або скачати з https://github.com/supabase/cli/releases

# Залогінитися
supabase login

# Підключитися до нового проекту
supabase link --project-ref xxxxx
# Замініть xxxxx на ваш Project Ref ID
# Введіть database password коли попросить

# Застосувати міграції
supabase db push
```

---

### Крок 4: Деплой Edge Functions на новий проект

Тепер ваш проект має всі таблиці. Час деплоїти Edge Functions!

**Через Supabase CLI:**

```bash
# Переконайтесь що ви в папці проекту
cd c:\Users\andre\Documents\some-future-project-3

# Якщо ще не зробили link:
supabase login
supabase link --project-ref xxxxx

# Деплой функцій
supabase functions deploy checkout
supabase functions deploy check-inventory

# Або всі разом:
supabase functions deploy
```

**Після деплою** побачите:
```
✓ Deployed Function checkout
✓ Deployed Function check-inventory
```

**Перевірте:**
1. Перейдіть в Supabase Dashboard → **Edge Functions**
2. Повинні бачити обидві функції

---

### Крок 5: Оновити .env з новими credentials

**Відкрийте файл** `.env` у вашому проекті

**Замініть старі credentials** на нові:

```env
# СТАРІ (від Bolt.new) - ВИДАЛИТИ
# VITE_SUPABASE_URL=https://biwyivgaojbbnxdaxkwa.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# НОВІ (ваш власний проект)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc... (ваш новий anon key)
```

**Збережіть файл!**

---

### Крок 6: Протестувати локально

```bash
# Встановити залежності (якщо ще не встановлені)
npm install

# Запустити локально
npm run dev
```

Відкрийте `http://localhost:5173`

**Протестуйте:**
1. ✅ Реєстрація нового користувача
2. ✅ Вхід в систему
3. ✅ Перегляд товарів (будуть пусті - це нормально)
4. ✅ Додавання товару в кошик

Якщо все працює - **успіх!** 🎉

---

### Крок 7: Оновити CORS в Edge Functions (Безпека)

Для локальної розробки CORS `*` це нормально, але **для продакшену** потрібно обмежити.

**Зараз можна пропустити цей крок**, повернетесь після деплою на Vercel.

---

### Крок 8: Деплой на Vercel

Тепер ваш проект готовий до продакшен деплою!

**Через GitHub (рекомендовано):**

```bash
# 1. Закомітити зміни в .env (НЕ КОМІТЬТЕ .env!)
# .env вже в .gitignore - це правильно!

git add .
git commit -m "Migrate to own Supabase project"

# 2. Запушити на GitHub
git push
```

**На Vercel:**

1. Перейдіть на [vercel.com](https://vercel.com)
2. **"Add New..." → "Project"**
3. **Імпортуйте** ваш GitHub репозиторій
4. **Додайте Environment Variables:**
   ```
   VITE_SUPABASE_URL = https://xxxxx.supabase.co (ваш НОВИЙ URL)
   VITE_SUPABASE_ANON_KEY = eyJhbGc... (ваш НОВИЙ key)
   ```
5. **Deploy**

Через 2-3 хвилини отримаєте URL:
```
https://your-project.vercel.app
```

---

### Крок 9: Налаштувати Auth Redirect URLs

Після деплою на Vercel:

1. **Supabase Dashboard** → Authentication → URL Configuration

2. **Додайте:**
   ```
   Site URL: https://your-project.vercel.app
   Redirect URLs:
   - https://your-project.vercel.app/**
   - http://localhost:5173/** (для локальної розробки)
   ```

3. **Збережіть**

---

### Крок 10: Оновити CORS в Edge Functions (Продакшен)

**Відкрийте:**
- `supabase/functions/checkout/index.ts`
- `supabase/functions/check-inventory/index.ts`

**Змініть:**
```typescript
// БУЛО (дозволяє всі домени):
const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

// СТАЛО (тільки ваш домен):
const corsHeaders = {
  'Access-Control-Allow-Origin': 'https://your-project.vercel.app',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}
```

**Або для підтримки і локалки і продакшену:**
```typescript
const corsHeaders = {
  'Access-Control-Allow-Origin': Deno.env.get('ALLOWED_ORIGIN') || '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}
```

**Деплой знову:**
```bash
supabase functions deploy
```

---

## ✅ Checklist міграції

- [ ] Створений власний проект на Supabase
- [ ] Отримані нові URL та anon key
- [ ] Застосована SQL міграція (через SQL Editor або CLI)
- [ ] Перевірено що всі таблиці створені (Table Editor)
- [ ] Задеплоєні Edge Functions (`supabase functions deploy`)
- [ ] Оновлений `.env` з новими credentials
- [ ] Протестовано локально (`npm run dev`)
- [ ] Закомічено зміни (БЕЗ .env!)
- [ ] Запушено на GitHub
- [ ] Деплоєно на Vercel
- [ ] Додані environment variables в Vercel
- [ ] Налаштовані Auth Redirect URLs в Supabase
- [ ] Оновлений CORS в Edge Functions
- [ ] Протестовано на продакшені

---

## 🎯 Що змінилося?

**ДО міграції:**
```
☁️ Bolt.new Supabase (НЕ ваш!)
└── База даних (немає доступу)

💻 Ваш код локально
└── Edge Functions (не можна деплоїти)
```

**ПІСЛЯ міграції:**
```
☁️ ВАШ Supabase проект
├── ✅ База даних (повний контроль)
├── ✅ Edge Functions (задеплоєні)
├── ✅ Auth система
└── ✅ Логи, метрики, все ваше!

☁️ Vercel
└── ✅ Frontend онлайн

💻 Локально
└── ✅ Розробка
```

---

## 🚨 Типові помилки

### "relation already exists"
**Причина:** Міграція вже була запущена раніше

**Рішення:** Це нормально! SQL використовує `IF NOT EXISTS`, тому повторний запуск безпечний.

### "Function not found" після деплою
**Причина:** Функції не деплоїлись або деплоїлись на інший проект

**Рішення:**
```bash
# Перевірте що підключені до правильного проекту
supabase projects list

# Якщо ні - переключіться:
supabase link --project-ref xxxxx

# Деплой знову
supabase functions deploy
```

### "Missing environment variables" на Vercel
**Причина:** Не додані змінні в Vercel Dashboard

**Рішення:**
1. Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Додайте `VITE_SUPABASE_URL` та `VITE_SUPABASE_ANON_KEY`
3. Redeploy: Deployments → ... → Redeploy

---

## 📊 Порівняння: Bolt.new vs Ваш проект

| Функція | Bolt.new проект | Ваш проект |
|---------|----------------|------------|
| Доступ до БД | ❌ Немає | ✅ Повний |
| Edge Functions | ❌ Не можна деплоїти | ✅ Можна |
| Логи і метрики | ❌ Немає | ✅ Є |
| Контроль версій БД | ❌ Немає | ✅ Міграції |
| Безпека даних | ⚠️ Можуть видалити | ✅ Ваш контроль |
| Кастомізація | ❌ Обмежена | ✅ Повна |
| Вартість | 🆓 Тимчасово | 🆓 Free tier |

---

## 💡 Корисні команди

```bash
# Перевірити статус підключення
supabase projects list

# Переключитися між проектами
supabase link --project-ref xxxxx

# Деплой функцій
supabase functions deploy

# Логи функцій
supabase functions logs checkout
supabase functions logs check-inventory

# Застосувати міграції
supabase db push

# Створити нову міграцію
supabase migration new my_migration_name
```

---

## 🎉 Готово!

Тепер у вас **повноцінний власний проект**:
- ✅ Ваша база даних на Supabase
- ✅ Ваші Edge Functions
- ✅ Frontend на Vercel
- ✅ Повний контроль над всім

**Наступні кроки:**
1. Додайте тестові дані в базу (категорії, товари)
2. Створіть адмін користувача (встановіть `is_admin = true` в profiles)
3. Протестуйте всі функції
4. Почніть розробку нових фіч!

---

## 📚 Додаткові ресурси

- [Supabase Documentation](https://supabase.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Edge Functions Guide](https://supabase.com/docs/guides/functions)
- [SQL Editor](https://supabase.com/docs/guides/database/overview)
